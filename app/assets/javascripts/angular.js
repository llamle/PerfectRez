var app = angular.module('PerfectRez', []);

app.controller('PerfectRezCtrl', ['$http', function($http){
  this.formData = '';
  currentThis = this;
  this.formAction = function ( {
    $http.get('')
  })
}])
